@model IEnumerable<KiemTra_TranDinhNguyen_B5.Models.HocPhan>

@{
    ViewBag.Title = "LayHocPhan";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>LayHocPhan</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenHP)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SoTinChi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenHP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoTinChi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaHP }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaHP }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaHP })
        </td>
    </tr>
}

</table>
